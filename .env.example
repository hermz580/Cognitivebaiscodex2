# ─────────────────────────────────────────────────────────────────────────────
# Cognitive Bias Codex — Environment Variables
# Copy this file to .env and fill in the values you want.
# .env is gitignored and will never be committed.
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Backends (all optional — the app works without any of these) ──────────

# Anthropic Claude (enables full vision scanning — best quality)
# Get yours at: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-...

# Hugging Face (enables cloud text analysis — free tier available)
# Get yours at: https://huggingface.co/settings/tokens
# Users can also connect their own token directly in the app UI.
# HF_TOKEN=hf_...

# Override the HF model used for text analysis (optional)
# HF_MODEL=mistralai/Mistral-7B-Instruct-v0.3

# ── Local LLM (optional, for home-network use) ───────────────────────────────

# URL of a local LLM API (e.g. LM Studio, Ollama, text-generation-webui)
# LOCAL_LLM_URL=http://localhost:1234/api/v1/chat
# LOCAL_LLM_MODEL=openai-gpt-oss-20b-abliterated-uncensored-neo-imatrix

# ── Server ───────────────────────────────────────────────────────────────────

# Port the server listens on (default 8000 locally, 7860 on HF Spaces)
# PORT=8000
