# Cognitive Bias Codex — Unified Server Requirements
# Covers: FastAPI REST + FastMCP AI Protocol + Prometheus + Wikipedia enrichment

# Web framework
fastapi>=0.109.0
uvicorn>=0.23.0

# MCP protocol (FastMCP for tool definitions + HTTP mounting)
mcp>=1.0.0
fastmcp>=2.0.0

# HTTP client (Wikipedia enrichment, async)
httpx>=0.26.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.0.0

# HTML scraping (fallback for non-Wikipedia URLs)
beautifulsoup4>=4.12.0

# Observability
prometheus-fastapi-instrumentator>=6.0.0

# Env vars
python-dotenv>=1.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
